@AGENTS.md

Claude Code reads only `CLAUDE.md`, so this file exists purely to pull in
`AGENTS.md` above — that is the single source of truth shared with every other
agent. Put project guidance there, not here.
